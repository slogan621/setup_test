.. highlight:: shell

============
Installation
============

At the command line::

    $ easy_install setup_test

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv setup_test
    $ pip install setup_test
